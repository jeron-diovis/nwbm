{
  "name": "",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./*": "./src/*"
  },
  "files": [
    "dist"
  ],
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "vite",
    "build:demo": "vite build",
    "build": "tsc -p tsconfig.build.json",
    "lint": "pnpm lint:js && pnpm lint:ts && pnpm lint:css",
    "preview": "vite preview",
    "stat": "./view-stats.sh",
    "lint:js": "eslint {src,demo} --ext .cjs,.js,.jsx,.ts,.tsx --no-error-on-unmatched-pattern",
    "lint:ts": "tsc --noEmit",
    "lint:css": "stylelint \"{src,demo}/**/*.{s,}css\" --aei",
    "fix": "pnpm lint:js --fix && pnpm lint:css --fix",
    "test": "vitest",
    "prepare": "npx symlink-dir ../../.vite .vite-root",
    "prepublishOnly": "pnpm test && pnpm build"
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "clsx": "^2.1.0",
    "lodash-es": "^4.17.21",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "utility-types": "^3.11.0"
  }
}
