# `ignoreFiles` option in .stylelintrc seems to not work properly.
# It resolves paths relatively to _root_ config file, not to working directory
#
# Alternatively, we can run stylelint with `--ignore-path=../../.stylelintignore` –
# but that's pretty tedious.

node_modules
dist

# any 3-party styles which has to be added as-is
src/styles/vendor

# files generated by storybook on installation
.storybook/example
